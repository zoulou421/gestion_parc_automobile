<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_fleet_maintenance_list" model="ir.ui.view">
        <field name="name">fleet.maintenance.list</field>
        <field name="model">fleet.maintenance</field>
        <field name="arch" type="xml">
            <list string="Entretiens">
                <field name="name"/>
                <field name="vehicle_id"/>
                <field name="maintenance_type"/>
                <field name="date"/>
                <field name="next_date"/>
                <field name="garage_id"/>
                <field name="cost" sum="Total"/>
                <field name="state" widget="badge"
                        decoration-success="state == 'done'"
                        decoration-info="state == 'in_progress'"
                        decoration-warning="state == 'planned'"/>
            </list>
        </field>
    </record>

    <record id="view_fleet_maintenance_form" model="ir.ui.view">
        <field name="name">fleet.maintenance.form</field>
        <field name="model">fleet.maintenance</field>
        <field name="arch" type="xml">
            <form string="Entretien">
                <header>
                    <button name="action_plan" string="Planifier" type="object"
                            invisible="state != 'draft'" class="oe_highlight"/>
                    <button name="action_start" string="Démarrer" type="object"
                            invisible="state != 'planned'" class="oe_highlight"/>
                    <button name="action_done" string="Terminer" type="object"
                            invisible="state != 'in_progress'" class="oe_highlight"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,planned,in_progress,done"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="vehicle_id"/>
                            <field name="maintenance_type"/>
                            <field name="date"/>
                            <field name="next_date"/>
                            <field name="odometer"/>
                        </group>
                        <group>
                            <field name="garage_id"/>
                            <field name="cost"/>
                        </group>
                    </group>
                    <group>
                        <field name="description" placeholder="Description des travaux..."/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_fleet_maintenance_search" model="ir.ui.view">
        <field name="name">fleet.maintenance.search</field>
        <field name="model">fleet.maintenance</field>
        <field name="arch" type="xml">
            <search string="Rechercher entretiens">
                <field name="vehicle_id"/>
                <field name="garage_id"/>
                <filter string="Planifiés" name="planned" domain="[('state', '=', 'planned')]"/>
                <filter string="En cours" name="in_progress" domain="[('state', '=', 'in_progress')]"/>
                <filter string="Terminés" name="done" domain="[('state', '=', 'done')]"/>
                <group expand="0" string="Grouper par">
                    <filter string="Véhicule" name="group_vehicle" context="{'group_by': 'vehicle_id'}"/>
                    <filter string="Type" name="group_type" context="{'group_by': 'maintenance_type'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="view_fleet_maintenance_calendar" model="ir.ui.view">
        <field name="name">fleet.maintenance.calendar</field>
        <field name="model">fleet.maintenance</field>
        <field name="arch" type="xml">
            <calendar string="Entretiens" date_start="date" color="maintenance_type" mode="month">
                <field name="vehicle_id"/>
                <field name="maintenance_type"/>
                <field name="state"/>
            </calendar>
        </field>
    </record>

    <record id="action_fleet_maintenance" model="ir.actions.act_window">
        <field name="name">Entretiens</field>
        <field name="res_model">fleet.maintenance</field>
        <field name="view_mode">list,form,calendar</field>
        <field name="search_view_id" ref="view_fleet_maintenance_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Planifier le premier entretien</p>
        </field>
    </record>

</odoo>